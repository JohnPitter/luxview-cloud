# =============================================================================
# LuxView Cloud Platform — .gitignore
# =============================================================================

# -- Environment & Secrets ----------------------------------------------------
.env
.env.local
.env.*.local

# -- Node.js ------------------------------------------------------------------
node_modules/
dist/
build/
.vite/
*.tsbuildinfo

# -- Go -----------------------------------------------------------------------
luxview-engine/tmp/
luxview-engine/engine
luxview-engine/luxview-engine-linux

# -- Docker volumes (local dev) -----------------------------------------------
volumes/
data/

# -- Traefik ------------------------------------------------------------------
traefik/acme.json
traefik/certs/

# -- Build artifacts ----------------------------------------------------------
*.exe
*.dll
*.so
*.dylib

# -- Logs ---------------------------------------------------------------------
*.log
logs/

# -- OS files -----------------------------------------------------------------
.DS_Store
Thumbs.db
Desktop.ini

# -- IDE ----------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~

# -- Temporary -----------------------------------------------------------------
tmp/
temp/
*.tmp

# -- Backups (local) ----------------------------------------------------------
backups/
*.sql.gz
*.archive.gz
*.rdb

# -- Proprietary (SoftNyx — NEVER commit) -------------------------------------
# Decompiled RakionLauncher sources + the packed load.bin live here locally only.
RakionLauncher/
*.bin

# -- Misc ----------------------------------------------------------------------
CLAUDE.md
.claude/
