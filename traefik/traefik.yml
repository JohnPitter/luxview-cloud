# =============================================================================
# Traefik v3 — Static Configuration (Production)
# =============================================================================

# -- Global -------------------------------------------------------------------
global:
  checkNewVersion: false
  sendAnonymousUsage: false

# -- Entrypoints --------------------------------------------------------------
entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"
    transport:
      respondingTimeouts:
        readTimeout: 300s
        # Generoso: downloads de client de jogo (centenas de MB) em conexões
        # lentas podem levar muitos minutos; senão o stream é cortado (EOF).
        writeTimeout: 1800s
        idleTimeout: 300s
    http:
      tls:
        certResolver: letsencrypt

# -- Certificate Resolvers (Let's Encrypt) ------------------------------------
certificatesResolvers:
  letsencrypt:
    acme:
      email: "admin@luxview.cloud"
      storage: /letsencrypt/acme.json
      httpChallenge:
        entryPoint: web

# -- Providers ----------------------------------------------------------------
providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true
  # HTTP provider is configured via compose CLI so Authorization can come
  # from INTERNAL_TOKEN (static YAML cannot interpolate env).

# -- API / Dashboard (disabled in production) ---------------------------------
api:
  dashboard: false

# -- Logging ------------------------------------------------------------------
log:
  level: WARN
  format: json

accessLog:
  filePath: /var/log/traefik/access.json
  format: json
  filters:
    statusCodes:
      - "200-599"
  fields:
    headers:
      defaultMode: drop
      names:
        User-Agent: keep
        Referer: keep
