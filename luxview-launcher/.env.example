# Origem da API da plataforma. Copie para .env (não versionado).
# Nunca use o IP cru da VPS aqui — o IP de cada jogo vem do catálogo
# (engine VPS_PUBLIC_IP → server_ip / auth_host).
LUXVIEW_BASE_URL=https://luxview.cloud
